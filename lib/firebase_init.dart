/// Firebase initialization is intentionally disabled in this repository.
/// The app uses mock authentication instead, which keeps the project
/// portable and avoids requiring extra platform-specific SDKs.
Future<void> initializeFirebaseIfSupported() async {
  // No-op placeholder for public GitHub publishing.
}
